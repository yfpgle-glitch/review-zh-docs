---
name: review-zh-docs
description: |
  审阅、修改和重写中文 Markdown 项目文档，包括 README、需求文档、PRD、技术方案、
  实施计划、验收说明、周报、复盘、工作总结以及 AI 生成或辅助生成的项目草稿。
  用于发现不可执行要求、缺失边界、无来源判断、伪结构、空话套话、格式噪音和
  对话残留；默认只给审阅意见，只有用户明确要求修改、整理或重写时才编辑文件。
---

# 中文文档审校

把中文项目文档改得可靠、具体、可执行、可验收、可交接。重点处理事实、边界、结构、工作结果，以及 AI 生成或辅助生成文档中的空话、机械总结和聊天痕迹；不做通用创作或人格化文风改写。

## 工作模式

- **审阅**：用户说“看看、检查、审一下、有什么问题”时，只指出问题，不修改文件。
- **修改**：用户明确说“修改、整理、更新”时，在原文件中修正可确认的问题。
- **重写**：用户明确要求重写，或现有结构无法局部修补时，在保留事实和边界的前提下重构。

模式明确时直接执行。无法判断用户是否授权修改时，停在审阅模式。

## 证据边界

先完整读取目标文档，再按文档中的事实主张决定核对范围：

- 审 README、模块说明或交接文档时，读取可用的现役规则、清单文件、运行脚本和文档明确引用的路径，确认安装、运行、测试、目录和配置说法。
- 审技术方案时，核对与结论直接相关的代码、接口、配置或现役设计文档；不要为了“深度”展开无关模块。
- 审需求、计划、周报和总结时，以用户提供的原始材料和可定位产物为依据；没有来源的业务事实不得补写。
- 命令、路径、日期、数字、接口和状态能低成本验证时应验证；不能验证时明确写出验证边界。
- 当前文件或配置与旧说明冲突时，以现役来源为准，并指出冲突。

缺少依据时使用 `待确认：...`，不要把合理猜测写成事实。

## 核心判断

按以下优先级判断问题：

1. **来源可靠性**：内容是否忠于材料、代码和当前配置。
2. **可执行性**：读者是否知道由谁、在什么条件下做什么。
3. **可验收性**：输入、输出、状态、边界、异常和完成标准是否明确。
4. **结构有效性**：标题、列表和表格是否真正组织信息。
5. **表达克制**：是否存在 AI 式套话、空泛铺垫、夸大结论、机械总结和对话残留。

不要因为措辞不够漂亮而掩盖事实错误、缺失约束或不可执行要求。

## 审校流程

1. 确认文档类型、目标读者和工作模式。
2. 读取全文，快速扫描标题层级、列表、表格、引用和命令。
3. 按证据边界核对会影响判断的事实，不扩大到无关范围。
4. 先找阻塞执行或验收的问题，再处理结构和措辞。
5. 审阅时给出可定位结论；修改时保留有效内容并直接编辑。
6. 修改后检查 Markdown 结构、链接、路径和命令；能运行的检查尽量运行。

需要文档类型检查清单或改写示例时，再读取 [references/review-patterns.md](references/review-patterns.md)。简单审阅不要加载它。

## 修改规则

- 优先局部修正；只有结构失效时才重写整节或全文。
- 保留原文中可确认的事实、有效标题和必要术语。
- 删除没有信息量的铺垫、夸大词和重复总结，不删除真实约束。
- 抽象动词必须落到对象、范围或完成标准；没有依据时改为待确认项。
- 不凭空增加角色、功能、数字、日期、接口、命令或产品承诺。
- 不把引用材料里的示例事实迁移到当前文档。
- 不顺带修改代码、配置或其他文档，除非用户明确授权。

## 常见问题

- **伪结构**：标题很多，但正文重复标题或只有空话。
- **抽象动作**：只有“优化、完善、提升、加强”，没有对象和完成标准。
- **缺少边界**：写了要做什么，却没有角色、异常、权限、非目标或验收条件。
- **无依据结论**：直接宣称方案更稳定、更先进或效果显著，没有来源或取舍。
- **模糊归因**：使用“业内认为、资料显示、专家指出”但没有可核对来源。
- **格式噪音**：滥用粗体、表情标题、内联小标题、长表格和重复总结。
- **对话残留**：保留“当然可以、下面是、希望这对你有帮助”等聊天措辞。

## 输出要求

### 审阅模式

先列问题，按影响从高到低排序。来自文件时使用路径和行号；没有行号时用章节名定位。

```markdown
- [严重] `path/to/file.md:42` 问题。影响：为什么会阻塞执行或导致误解。建议：如何处理。
- [中等] `## 实施计划` 问题。建议：如何处理。
```

只在确有内容时补充待确认事项、可保留内容或验证范围。没有明显问题时直接说明，并列出尚未核对的风险。用户没有要求评分时，不输出分数。

### 修改或重写模式

直接编辑文件。修改后简要说明实际变化、已验证内容和仍待确认事项，不重复整篇文档，不声称未执行的检查已经通过。
